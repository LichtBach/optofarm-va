{
 "note": "Node bodies as live before 2026-09-25 (versionId e247f42b-f97c-41db-8a46-fb9468ea2dcc). Paste each jsCode back to roll back.",
 "nodes": [
  {
   "name": "CA Store Token",
   "jsCode": "const r = $input.first().json;\nif (r.statusCode !== 200 || !r.body || !r.body.token) {\n  return [{ json: { _error: true, success: false, error: 'auth_failed', status: r.statusCode,\n    hint: 'Could not authenticate to the clinic system. Tell the caller to try again shortly.' } }];\n}\nconst sd = $getWorkflowStaticData('global');\nsd.token = r.body.token;\nsd.tokenTs = Date.now();\nreturn [{ json: { token: sd.token } }];"
  },
  {
   "name": "CA Match Calendars",
   "jsCode": "// ---- calendar name vs service (2026-09-16) ----\n// One calendar of 30 carries its service in its NAME: \"Dr. Prof. Szekely Attila  - Consiliere /\n// Terapie psiho-ortoptica\" (note the double space). Returning that whole string as `doctor` made the\n// agent read the service suffix out loud, and pass it back into booking. Split it: `doctor` is the\n// speakable name, `service` is the suffix, and the name matcher resolves either form to the same\n// single calendar (verified against all 30 live names).\nconst splitName = raw => {\n  const s = (raw || '').toString().replace(/\\s+/g, ' ').trim();\n  const m = s.match(/^(.*?)\\s+[-\\u2013\\u2014]\\s+(.*)$/);\n  // No `service` key at all when there is no suffix, so 29 of 30 calendars are unchanged.\n  return m ? { doctor: m[1].trim(), service: m[2].trim() } : { doctor: s };\n};\nconst q = $('CA Validate Input').first().json;\nconst resp = $input.first().json;\nif (resp.statusCode !== 200 || !resp.body || !resp.body.Calendars) {\n  return [{ json: { _error: true, success: false, error: 'evolvo_api_error', step: 'get_info',\n    status: resp.statusCode } }];\n}\nconst norm = s => (s || '').toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '');\n// Titles a caller may put before OR after a name, dropped from both sides of the comparison.\n// words() has already lowercased, stripped diacritics and split on [^a-z0-9], so a\n// token is plain a-z0-9 and [a-z]* is the right open ending (and needs no escaping).\n// Hungarian is agglutinative and puts the title last, carrying the case ending - doktornohoz,\n// doktornot, doktornovel, doktor urhoz, asszonyhoz - so a fixed word list can never cover it\n// (raised by the ElevenLabs side 2026-09-16: \"Ilovan doktornohoz\" used to survive as a name token\n// and lose the match). Anything built on one of these stems is a title, whatever is glued on;\n// the stems are long enough that no real provider or branch token starts with one, and the short\n// ambiguous ones stay exact. Verified against all 30 calendar names, every workstation and every\n// branch alias: nothing else is stripped.\nconst TITLE = new RegExp('^(?:' + [\n  'dr', 'dra', 'drd', 'dna', 'dl', 'dsoara',                    // RO abbreviations\n  'doctor[a-z]*', 'doamn[a-z]*', 'domn[a-z]*', 'medic[a-z]*',              // RO, any case ending\n  'prof', 'profesor[a-z]*', 'professzor[a-z]*',                     // either language\n  'doktor[a-z]*', 'urno[a-z]*', 'asszony[a-z]*', 'kisasszony[a-z]*',      // HU, any case ending\n  // HU \"ur\" and \"no\" take the same endings but are too short to prefix-match safely\n  // (ur[a-z]* would swallow a future provider called Urban), so their forms are spelled out.\n  'ur', 'urat', 'urak', 'urnak', 'urhoz', 'urnal', 'urral', 'urrol', 'urtol', 'urert', 'urig',\n  'no', 'not', 'nok', 'nohoz', 'nonek', 'nonel', 'novel', 'nore',\n].join('|') + ')$');\nconst words = s => norm(s).split(/[^a-z0-9]+/).filter(w => w && !TITLE.test(w));\nconst lev = (a, b) => { const m = a.length, n = b.length; if (!m) return n; if (!n) return m;\n  let prev = Array.from({ length: n + 1 }, (_, j) => j);\n  for (let i = 1; i <= m; i++) { const cur = [i];\n    for (let j = 1; j <= n; j++) cur[j] = Math.min(prev[j] + 1, cur[j - 1] + 1, prev[j - 1] + (a[i - 1] === b[j - 1] ? 0 : 1));\n    prev = cur; }\n  return prev[n]; };\n// A spoken/ASR name token matches a calendar name token when equal, a prefix (>=3 chars), or within a small edit distance.\nconst tokEq = (q, t) => q === t || (q.length >= 3 && (t.startsWith(q) || q.startsWith(t))) || (q.length >= 3 && lev(q, t) <= (q.length >= 5 ? 2 : 1));\nconst nameMatch = (query, name) => { const qw = words(query), nw = words(name); return qw.length > 0 && qw.every(q => nw.some(t => tokEq(q, t))); };\n// Provider kind, read off the calendar name as evolvo writes it.\n// \"Optometrist Bodi Ildiko\" -> optometrist. \"Dr. Baricz Anna\" -> doctor.\n// A name carrying a service suffix is a SPECIALTY calendar, not general eye care:\n// \"Dr. Prof. Szekely Attila  - Consiliere / Terapie psiho-ortoptica\" -> vision_therapy.\n// Anything unrecognised is treated as a doctor: the safe direction to fail in.\nconst providerKind = name => {\n  const n = norm(name).trim();\n  const svc = norm(splitName(name).service);\n  if (svc) {\n    if (/ortopt/.test(svc)) return 'vision_therapy';\n    return 'specialty';\n  }\n  if (/^optometrist\\b/.test(n)) return 'optometrist';\n  return 'doctor';\n};\n// General eye care - what a caller who has not asked for anything specific means.\nconst GENERAL = ['doctor', 'optometrist'];\nconst isGeneral = name => GENERAL.includes(providerKind(name));\n// A specialty provider is only ever surfaced when the caller asked for one: by naming the person,\n// or by sending that exact provider_type. Never as \"the earliest free slot at this branch\"\n// (which is how the psycho-orthoptics calendar became the default answer for Republicii).\nconst wantsSpecialty = Boolean(q.provider_type) && !GENERAL.includes(q.provider_type);\n// City / branch aliases as callers say them (RO, HU, ASR variants) -> how evolvo writes the workstation.\nconst ALIAS = [[/^(targu|tirgu|tg\\.?|tg)\\s*mures$/, 'tg. mures'], [/^marosvasarhely$/, 'tg. mures'], [/^mures$/, 'tg. mures'],\n  [/^(regin|szaszregen|reghin)$/, 'reghin'], [/^(szovata|sovata)$/, 'sovata'],\n  [/^(posta utca|postei|posta)$/, 'postei'], [/^(rozsak tere|trandafirilor)$/, 'trandafirilor'],\n  [/^(koztarsasag|szentgyorgyter|republicii)$/, 'republicii'], [/^(dozsa|doja|dozsa gyorgy)$/, 'doja'],\n  [/^(bulevard|bulevardul|1 decembrie 1918)$/, '1 dec 1918 nr 49']];\n// Hungarian/Romanian branch names as callers say them, rewritten INSIDE a longer string.\n// The ALIAS table above only fires on a whole-string match, so \"Marosvasarhely, Rozsak tere\"\n// - a Hungarian caller naming city and square together, which is the natural way to say it -\n// matched nothing at all. Longest phrase first, so \"dozsa gyorgy\" wins over \"dozsa\".\n// Targets are spelled as the tokens evolvo actually has in `workstation`.\nconst PHRASES = [\n  ['1 decembrie 1918', '1 dec 1918'], ['koztarsasag tere', 'republicii'],\n  ['szentgyorgy ter', 'republicii'], ['szentgyorgyter', 'republicii'],\n  ['koztarsasag ter', 'republicii'], ['koztarsasag', 'republicii'],\n  ['rozsak tere', 'trandafirilor'], ['rozsak ter', 'trandafirilor'],\n  ['dozsa gyorgy', 'doja'], ['posta utca', 'postei'], ['iskola utca', 'scolii'],\n  ['marosvasarhely', 'tg mures'], ['targu mures', 'tg mures'], ['tirgu mures', 'tg mures'],\n  ['szaszregen', 'reghin'], ['szovata', 'sovata'], ['dozsa', 'doja'], ['posta', 'postei'],\n  ['fo utca', 'principala'], ['fo ut', 'principala'],\n  ['bulevardul', '1 dec 1918'], ['bulevard', '1 dec 1918'],\n];\n// Generic street/square nouns carry no branch information: \"Dozsa Gyorgy utca\" has to reach\n// the same place as \"Dozsa\". Dropped from the QUERY only, never from the workstation, and only\n// when something real survives - so a bare \"utca\" still matches nothing instead of everything.\nconst NOISE = new Set(['utca', 'ut', 'uti', 'tere', 'ter', 'strada', 'str', 'sugarut', 'korut',\n  'nr', 'szam', 'bld', 'bulevard', 'piata', 'piac']);\nconst rewrite = n => { let out = n;\n  for (const [from, to] of PHRASES) out = out.split(from).join(to);\n  out = out.replace(/\\s+/g, ' ').trim();\n  const kept = out.split(' ').filter(w => w && !NOISE.has(w.replace(/\\.$/, '')));\n  return kept.length ? kept.join(' ') : out; };\nconst canonPlace = s => { const n = norm(s).replace(/[^a-z0-9. ]+/g, ' ').replace(/\\s+/g, ' ').trim(); for (const [re, to] of ALIAS) if (re.test(n)) return to; return rewrite(n); };\nconst placeMatch = (query, ws) => { const qn = canonPlace(query), wn = norm(ws); if (!qn) return true; if (wn.includes(qn)) return true; const qw = words(qn); return qw.length > 0 && qw.every(q => words(ws).some(t => tokEq(q, t))); };\nconst cals = resp.body.Calendars;\nif (!q.doctor && !q.location && !q.city && !wantsSpecialty) {\n  return [{ json: { _error: true, success: false, error: 'missing_target',\n    message: 'Ask the caller which branch, city or doctor they want before checking availability. Do not guess one.',\n    available_locations: [...new Set(cals.map(c => c.workstation))] } }];\n}\nlet m = cals;\nlet byDoctor = null;\nif (q.doctor) { byDoctor = m.filter(c => nameMatch(q.doctor, c.name)); m = byDoctor; }\nif (q.city) m = m.filter(c => placeMatch(q.city, c.workstation));\nif (q.location) m = m.filter(c => placeMatch(q.location, c.workstation));\nif (m.length === 0 && byDoctor && byDoctor.length > 0) {\n  return [{ json: { _error: true, success: false, error: 'doctor_not_at_location',\n    message: 'This doctor exists but not at the requested branch. Tell the caller where the doctor works and ask whether that branch suits, or offer another doctor at the requested branch; then call again.',\n    doctor: [...new Set(byDoctor.map(c => splitName(c.name).doctor))],\n    doctor_locations: [...new Set(byDoctor.map(c => c.workstation))],\n    doctors_at_requested_location: [...new Set(cals.filter(c => placeMatch(q.location || q.city, c.workstation) && isGeneral(c.name)).map(c => splitName(c.name).doctor))] } }];\n}\n// ---- specialty gate (2026-09-16) ----\n// Drop specialty calendars unless this caller asked for one. Runs before the kind filter and after\n// the location filters, so `doctor_not_at_location` still reports the truth about a named person.\nif (!q.doctor && !wantsSpecialty) {\n  const general = m.filter(c => isGeneral(c.name));\n  if (general.length === 0 && m.length > 0) {\n    return [{ json: { _error: true, success: false, error: 'only_specialty_providers_here',\n      message: 'That branch has no general eye doctor or optometrist - only a specialty provider, who must NOT be offered unless the caller asked for that specific service. Offer another branch instead.',\n      requested_location: q.location || q.city || '',\n      specialty_providers: [...new Set(m.map(c => splitName(c.name).doctor))],\n      specialty_services: [...new Set(m.map(c => splitName(c.name).service).filter(Boolean))],\n      other_locations: [...new Set(cals.filter(c => isGeneral(c.name)).map(c => c.workstation))] } }];\n  }\n  m = general;\n}\n// Provider-kind filter. Deliberately skipped when the caller named a specific person:\n// an explicit request for Dr. X always beats the visit-type heuristic, so a stray\n// provider_type alongside a doctor name can never strand the caller.\nif (q.provider_type && !q.doctor) {\n  const byType = m.filter(c => providerKind(c.name) === q.provider_type);\n  if (byType.length === 0 && m.length > 0) {\n    return [{ json: { _error: true, success: false, error: 'no_provider_of_type',\n      message: 'Nobody of the requested kind works at that branch. Say so plainly, then offer what IS available there and call again.',\n      requested_provider_type: q.provider_type,\n      requested_location: q.location || q.city || '',\n      available_provider_types: [...new Set(m.map(c => providerKind(c.name)))],\n      available_doctors: [...new Set(m.filter(c => isGeneral(c.name)).map(c => splitName(c.name).doctor))],\n      available_locations: [...new Set(m.map(c => c.workstation))] } }];\n  }\n  m = byType;\n}\nif (m.length === 0) {\n  return [{ json: { _error: true, success: false, error: 'no_match',\n    message: 'No doctor/location matches. Offer the caller these options (at most three at a time) and call again.',\n    available_doctors: [...new Set(cals.filter(c => isGeneral(c.name)).map(c => splitName(c.name).doctor))],\n    available_locations: [...new Set(cals.map(c => c.workstation))] } }];\n}\n// A branch alone can match up to ~8 calendars; query them all so `earliest` works for no-preference callers.\nif (m.length > 10) {\n  return [{ json: { _error: true, success: false, error: 'too_many_matches',\n    message: 'Too many calendars match. Ask the caller to narrow down by doctor or location.',\n    matching_doctors: [...new Set(m.map(c => splitName(c.name).doctor))],\n    matching_locations: [...new Set(m.map(c => c.workstation))] } }];\n}\n// ---- diagnosis-first rule (2026-09-16, clinic decision) ----\n// The clinic requires an eye doctor to examine the caller and recommend the therapy BEFORE a\n// psycho-orthoptics appointment is made. So even when the caller asked for the specialty provider\n// by name or by provider_type, the slots come back wrapped in this rule rather than as a plain\n// offer, together with the general providers at the same branch to send them to first.\n// book_appointment enforces the same rule server-side (error `diagnosis_required`).\nconst specialtyRule = c => {\n  const svc = splitName(c.name).service;\n  const here = cals.filter(x => x.workstation === c.workstation && isGeneral(x.name));\n  return {\n    requires_prior_diagnosis: true,\n    service: svc,\n    instruction: 'Do NOT offer these slots yet. The clinic requires an eye doctor to examine the caller and recommend this therapy first. Say that a consultation with an eye doctor comes first, and offer one of the doctors below (call this tool again with provider_type doctor for their times). Only if the caller says a doctor has ALREADY examined them and recommended this therapy may you offer these slots - and then book_appointment must be called with diagnosis_confirmed true, or it will refuse.',\n    doctors_for_diagnosis: [...new Set(here.map(x => splitName(x.name).doctor))],\n    locations_for_diagnosis: here.length ? [c.workstation]\n      : [...new Set(cals.filter(x => isGeneral(x.name)).map(x => x.workstation))],\n  };\n};\nreturn m.map(c => ({ json: { calendarid: c.calendarid, ...splitName(c.name), location: c.workstation,\n  slot_duration: c.slot_duration, provider_type: providerKind(c.name),\n  ...(isGeneral(c.name) ? {} : { _specialty_rule: specialtyRule(c) }) } }));"
  },
  {
   "name": "CA Format Slots",
   "jsCode": "// ---- spoken date labels (2026-09-15) ----\n// A live call read \"2026-09-16 15:40\" out as \"astazi, marti 15 septembrie\" and the next slot\n// (2026-09-17 10:00) as \"maine, miercuri 16 septembrie\" - the LLM was converting ISO dates to\n// spoken dates itself and was a day out, twice in one call, weekday name included. It never has to\n// do that arithmetic: every date we emit now carries date_spoken (weekday + day + month in\n// English, for the agent to translate) and relative_day (today / tomorrow / in N days), so\n// speaking a date is a translation, not a calculation.\nconst _WD = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];\nconst _MO = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];\nconst _todayISO = new Date().toLocaleDateString('en-CA', { timeZone: 'Europe/Bucharest' });\nconst _dayNo = iso => Math.round(Date.parse(iso + 'T12:00:00Z') / 86400000);\nfunction dateLabels(iso) {\n  iso = (iso || '').toString().slice(0, 10);\n  if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(iso)) return {};\n  const d = new Date(iso + 'T12:00:00Z');\n  const diff = _dayNo(iso) - _dayNo(_todayISO);\n  return {\n    date_spoken: _WD[d.getUTCDay()] + ' ' + d.getUTCDate() + ' ' + _MO[d.getUTCMonth()],\n    relative_day: diff === 0 ? 'today' : diff === 1 ? 'tomorrow' : diff === 2 ? 'the day after tomorrow'\n      : diff < 0 ? 'IN THE PAST - do not offer this' : 'in ' + diff + ' days',\n  };\n}\nconst SPEAK_DATES = 'Never work out a date yourself and never re-read the ISO date: say date_spoken translated into the caller\\'s language, and say today or tomorrow only when relative_day says so.';\nconst cals = $('CA Match Calendars').all();\nconst resps = $input.all();\nconst q = $('CA Validate Input').first().json;\nconst out = [];\nlet earliest = null;\nlet requestedDay = null;\nfor (let i = 0; i < resps.length; i++) {\n  const cal = cals[i] ? cals[i].json : {};\n  const r = resps[i].json;\n  if (r.statusCode !== 200 || !r.body || !r.body.Records) {\n    out.push({ doctor: cal.doctor, ...(cal.service ? { service: cal.service } : {}), location: cal.location,\n      provider_type: cal.provider_type, error: 'no_availability_data', status: r.statusCode });\n    continue;\n  }\n  const recs = (r.body.Records || []).filter(rec => (rec.free_timespace || []).length > 0);\n  const days = recs.slice(0, 4).map(rec => ({\n    date: rec.wday,\n    ...dateLabels(rec.wday),\n    free_times: (rec.free_timespace || []).slice(0, 8).map(s => s.slot),\n    total_free_slots: (rec.free_timespace || []).length,\n  }));\n  for (const rec of recs) {\n    const t = (rec.free_timespace || [])[0];\n    if (!t) continue;\n    const key = rec.wday + ' ' + t.slot;\n    if (!earliest || key < earliest.date + ' ' + earliest.time) earliest = { date: rec.wday, ...dateLabels(rec.wday), time: t.slot, doctor: cal.doctor, ...(cal.service ? { service: cal.service } : {}), location: cal.location, provider_type: cal.provider_type };\n    if (q.requested_date && rec.wday === q.requested_date) {\n      const cand = { date: rec.wday, ...dateLabels(rec.wday), time: t.slot, doctor: cal.doctor, ...(cal.service ? { service: cal.service } : {}), location: cal.location, provider_type: cal.provider_type, free_times: (rec.free_timespace || []).slice(0, 8).map(s => s.slot) };\n      if (!requestedDay || cand.time < requestedDay.time) requestedDay = cand;\n    }\n  }\n  out.push({ doctor: cal.doctor, ...(cal.service ? { service: cal.service } : {}), location: cal.location,\n    provider_type: cal.provider_type, slot_duration_minutes: cal.slot_duration, available_days: days });\n}\n// ---- found, but nothing free (2026-09-16, asked for by the ElevenLabs side) ----\n// A matched provider with no free time used to come back as nothing but `available_days: []` and a\n// missing `earliest`. An absence is a weak signal for an LLM: on conv_0401m2mx22pveh5tkr6qy9fq3eat\n// the agent filled the gap by telling a caller that Dr. Ilovan Anca does not work at Postei, where\n// she does. So the case is now named on the result, at the top level, and first in the note.\n// A calendar whose lookup FAILED keeps its own `error` and is deliberately not counted here.\nfor (const r of out) if (!r.error && !(r.available_days || []).length) r.no_free_slots = true;\nconst nothingFree = out.filter(r => r.no_free_slots);\nconst haveSlots = out.filter(r => !r.error && (r.available_days || []).length);\nconst who = rs => rs.map(r => r.doctor + (r.location ? ' (' + r.location + ')' : '')).join(', ');\n\nconst res = { success: true, today: _todayISO, date_searched_from: q.date_from, results: out };\nif (nothingFree.length && !haveSlots.length) res.no_free_slots = true;\n// A specialty provider can only be reached deliberately (named, or provider_type), and when one is\n// in the results the clinic's diagnosis-first rule governs the whole answer - so it goes top level,\n// where the agent cannot miss it, rather than per result.\nconst rule = cals.map(c => c.json && c.json._specialty_rule).find(Boolean);\nif (rule) res.specialty_booking_rule = rule;\n// Echoed only when the filter actually ran; a named doctor bypasses it (see CA Match Calendars).\nif (q.provider_type && !q.doctor) res.provider_type_filter = q.provider_type;\nif (earliest) res.earliest = earliest;\nif (q.requested_date) {\n  res.requested_date = q.requested_date;\n  res.requested_date_status = requestedDay ? 'available' : 'not_available';\n  if (requestedDay) res.requested_date_slots = requestedDay;\n}\nif (rule) res.note = 'READ specialty_booking_rule FIRST - these slots must not be offered until an eye doctor has diagnosed the caller and recommended this therapy. ';\nelse res.note = '';\nif (nothingFree.length && !haveSlots.length) {\n  res.note += 'NO FREE SLOTS: ' + who(nothingFree) + (nothingFree.length > 1 ? ' were' : ' was')\n    + ' found and ' + (nothingFree.length > 1 ? 'do' : 'does') + ' work there, but '\n    + (nothingFree.length > 1 ? 'have' : 'has') + ' no free time from ' + q.date_from\n    + ' to the end of the ~15 working days searched. Say exactly that, then offer a later date (call again with date_from) or another provider or branch. NEVER tell the caller they do not work there, and never present the name in results as a different person from the one they asked for - it is the same person, spelled as the clinic stores it. ';\n} else if (nothingFree.length) {\n  res.note += 'PARTLY FREE: ' + who(nothingFree) + ' matched but ' + (nothingFree.length > 1 ? 'have' : 'has')\n    + ' no free time in the searched window (see no_free_slots on those results). Offer one of the providers that does have times; do NOT say the others are absent from that branch. ';\n}\nres.note += 'Times are local Romania time (24h). earliest = the single earliest free slot across all matched calendars; use it for urgent callers or callers with no preference. The system searched the next ~15 working days of each calendar starting from date_searched_from, so nothing here falls before that date and nothing here is today. ' + SPEAK_DATES;\nreturn [{ json: res }];"
  },
  {
   "name": "BOOK Validate Input",
   "jsCode": "const b = ($input.first().json.body) || {};\nconst str = x => (typeof x === 'string' ? x.trim() : '');\nconst missing = [];\nconst full_name = str(b.full_name);\nconst PLACEHOLDER = ['necunoscut','ismeretlen','unknown','n/a','na','-','pacient','patient','beteg'];\nif (!full_name || full_name.replace(/[^\\p{L}]/gu, '').length < 2 || PLACEHOLDER.includes(full_name.toLowerCase())) missing.push('full_name (real full name, never empty or a placeholder)');\nconst phone = str(b.phone).replace(/[\\s\\-\\.]/g, ''); if (!phone) missing.push('phone');\nconst date = str(b.date); if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(date)) missing.push('date (YYYY-MM-DD)');\nlet time = str(b.time); if (/^\\d{1}:\\d{2}$/.test(time)) time = '0' + time;\nif (!/^\\d{2}:\\d{2}$/.test(time)) missing.push('time (HH:MM, 24h)');\nconst doctor = str(b.doctor), city = str(b.city), location = str(b.location);\nif (!doctor && !location && !city) missing.push('doctor or location');\nlet email = str(b.email); if (!/.+@.+\\..+/.test(email)) email = 'noemail@optofarm.ro';\nconst problem = str(b.problem);\nconst conversation_id = str(b.conversation_id || b.idempotency_key) || 'anon';\n// Clinic rule (2026-09-16): a psycho-orthoptics appointment may only be made after an eye doctor has\n// examined the caller and recommended the therapy. The agent reports that with diagnosis_confirmed.\n// It is only consulted when the chosen slot turns out to belong to a specialty calendar, so sending\n// it for an ordinary booking is harmless and leaving it out of one is fine - see BOOK Find Slot.\nconst diagnosis_confirmed = b.diagnosis_confirmed === true || String(b.diagnosis_confirmed).toLowerCase() === 'true';\nif (missing.length) {\n  return [{ json: { _error: true, success: false, error: 'missing_fields', missing,\n    hint: 'Ask the caller for the missing details, then call this tool again.' } }];\n}\n\n// ---- phone read-back gate (server-side) ----\n// The agent must read the number back digit by digit and get the caller's yes before this call.\n// It reports that with phone_confirmed=true. A call without it is refused with a read-back instruction;\n// a call that flips the flag less than 5 s after the refusal is refused again (no real read-back could fit).\n{\n  const sd = $getWorkflowStaticData('global'); sd.phoneGate = sd.phoneGate || {};\n  const now = Date.now();\n  for (const k of Object.keys(sd.phoneGate)) if (now - sd.phoneGate[k] > 2 * 3600 * 1000) delete sd.phoneGate[k];\n  const conv = str(b.conversation_id || b.idempotency_key) || 'anon';\n  const gateKey = conv + '|' + phone;\n  const confirmed = b.phone_confirmed === true || String(b.phone_confirmed).toLowerCase() === 'true';\n  const firstRefusal = sd.phoneGate[gateKey];\n  if (!confirmed || (firstRefusal && now - firstRefusal < 5000)) {\n    if (!firstRefusal) sd.phoneGate[gateKey] = now;\n    const d = phone.split('');\n    const groups = [];\n    for (let i = 0; i < d.length; i += 3) groups.push(d.slice(i, i + 3).join(', '));\n    return [{ json: { _error: true, success: false, error: 'confirm_phone_first', phone,\n      read_back: groups.join(' — '),\n      message: 'Nothing was done yet. First read this phone number back to the caller digit by digit, ask whether it is correct, and END your turn. Only after the caller answers yes, call this tool again with exactly the same details and phone_confirmed set to true. If the caller corrects the number, call again with the corrected number and phone_confirmed true after they confirm it.' } }];\n  }\n}\n\n// ---- slot acceptance gate (server-side) ----\n// The agent must have offered this exact day+time and the caller must have accepted it (slot_accepted=true).\n{\n  const accepted = b.slot_accepted === true || String(b.slot_accepted).toLowerCase() === 'true';\n  if (!accepted) {\n    return [{ json: { _error: true, success: false, error: 'offer_slot_first', date, time,\n      message: 'Nothing was booked. First offer this day and time to the caller (weekday, date, hour, doctor, branch), ask whether it suits, and END your turn. Only after the caller accepts, call this tool again with the same details and slot_accepted set to true.' } }];\n  }\n}\nconst today = new Date().toLocaleDateString('en-CA', { timeZone: 'Europe/Bucharest' });\nif (date < today) {\n  return [{ json: { _error: true, success: false, error: 'date_in_past', requested_date: date,\n    today } }];\n}\n// ---- remember the confirmed number for this call ----\n// Once a caller has dictated a number and confirmed the read-back, no later step in the same call\n// should make them dictate it again. The number is kept against the conversation id so log_request\n// can recover it server-side instead of relying on the agent to remember not to ask.\n{\n  const sd = $getWorkflowStaticData('global'); sd.confirmedPhone = sd.confirmedPhone || {};\n  const now = Date.now();\n  for (const k of Object.keys(sd.confirmedPhone)) if (now - sd.confirmedPhone[k].ts > 6 * 3600 * 1000) delete sd.confirmedPhone[k];\n  const conv = str(b.conversation_id || b.idempotency_key);\n  if (conv) sd.confirmedPhone[conv] = { phone, ts: now };\n}\nreturn [{ json: { full_name, phone, email, date, time, doctor, city, location,\n  problem, conversation_id, diagnosis_confirmed } }];"
  },
  {
   "name": "BOOK Store Token",
   "jsCode": "const r = $input.first().json;\nif (r.statusCode !== 200 || !r.body || !r.body.token) {\n  return [{ json: { _error: true, success: false, error: 'auth_failed', status: r.statusCode,\n    hint: 'Could not authenticate to the clinic system. Tell the caller to try again shortly.' } }];\n}\nconst sd = $getWorkflowStaticData('global');\nsd.token = r.body.token;\nsd.tokenTs = Date.now();\nreturn [{ json: { token: sd.token } }];"
  },
  {
   "name": "BOOK Find Slot",
   "jsCode": "const q = $('BOOK Validate Input').first().json;\nconst cals = $('BOOK Match Calendars').all().map(i => i.json);\nconst resps = $input.all().map(i => i.json);\nlet apiErrors = 0, dayMisses = 0;\nconst altDays = [], freeThatDay = [], matches = [];\nfor (let i = 0; i < resps.length; i++) {\n  const r = resps[i]; const cal = cals[i] || {};\n  if (r.statusCode !== 200 || !r.body || !r.body.Records) { apiErrors++; continue; }\n  const day = (r.body.Records || []).find(x => x.wday === q.date);\n  if (!day) {\n    dayMisses++;\n    for (const x of (r.body.Records || []).slice(0, 3)) altDays.push({ date: x.wday, doctor: cal.doctor, sample_times: (x.free_timespace || []).slice(0, 5).map(s => s.slot) });\n    continue;\n  }\n  const slot = (day.free_timespace || []).find(s => s.slot === q.time);\n  if (slot) matches.push({ slotid: slot.slotid, date: q.date, time: q.time, calendarid: cal.calendarid, doctor: cal.doctor, service: cal.service || '', location: cal.location });\n  else for (const s of (day.free_timespace || [])) freeThatDay.push({ time: s.slot, doctor: cal.doctor });\n}\nif (matches.length) {\n  // ---- diagnosis-first gate (2026-09-16, clinic decision) ----\n  // Same rule check_availability announces in specialty_booking_rule, enforced where it counts.\n  // Only a slot on a specialty calendar is affected; every ordinary booking is untouched.\n  const won = matches[0];\n  if (won.service && !q.diagnosis_confirmed) {\n    return [{ json: { _error: true, success: false, error: 'diagnosis_required',\n      requested: { doctor: won.doctor, service: won.service, date: won.date, time: won.time, location: won.location },\n      message: 'NOTHING WAS BOOKED. ' + won.service + ' is only booked after an eye doctor has examined the caller and recommended this therapy. Ask the caller whether a doctor has already done so. If not, say the consultation comes first and offer an eye doctor instead (check_availability with provider_type doctor). If the caller confirms they already have that diagnosis or recommendation, call this tool again with the same details and diagnosis_confirmed set to true.' } }];\n  }\n  return [{ json: won }];\n}\nif (apiErrors === resps.length) {\n  return [{ json: { _error: true, success: false, error: 'evolvo_api_error', step: 'get_work_days' } }];\n}\nif (dayMisses === resps.length - apiErrors) {\n  altDays.sort((a, b) => a.date.localeCompare(b.date));\n  return [{ json: { _error: true, success: false, error: 'date_not_available',\n    requested_date: q.date, alternative_days: altDays.slice(0, 3),\n    hint: 'Offer the caller one of the alternative days.' } }];\n}\nfreeThatDay.sort((a, b) => a.time.localeCompare(b.time));\nreturn [{ json: { _error: true, success: false, error: 'time_not_available',\n  requested: q.date + ' ' + q.time,\n  free_times_that_day: freeThatDay.slice(0, 12),\n  hint: 'Offer the caller one of the free times (say the doctor too).' } }];"
  },
  {
   "name": "BOOK Format Booking",
   "jsCode": "// ---- spoken date labels (2026-09-15) ----\n// A live call read \"2026-09-16 15:40\" out as \"astazi, marti 15 septembrie\" and the next slot\n// (2026-09-17 10:00) as \"maine, miercuri 16 septembrie\" - the LLM was converting ISO dates to\n// spoken dates itself and was a day out, twice in one call, weekday name included. It never has to\n// do that arithmetic: every date we emit now carries date_spoken (weekday + day + month in\n// English, for the agent to translate) and relative_day (today / tomorrow / in N days), so\n// speaking a date is a translation, not a calculation.\nconst _WD = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];\nconst _MO = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];\nconst _todayISO = new Date().toLocaleDateString('en-CA', { timeZone: 'Europe/Bucharest' });\nconst _dayNo = iso => Math.round(Date.parse(iso + 'T12:00:00Z') / 86400000);\nfunction dateLabels(iso) {\n  iso = (iso || '').toString().slice(0, 10);\n  if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(iso)) return {};\n  const d = new Date(iso + 'T12:00:00Z');\n  const diff = _dayNo(iso) - _dayNo(_todayISO);\n  return {\n    date_spoken: _WD[d.getUTCDay()] + ' ' + d.getUTCDate() + ' ' + _MO[d.getUTCMonth()],\n    relative_day: diff === 0 ? 'today' : diff === 1 ? 'tomorrow' : diff === 2 ? 'the day after tomorrow'\n      : diff < 0 ? 'IN THE PAST - do not offer this' : 'in ' + diff + ' days',\n  };\n}\nconst SPEAK_DATES = 'Never work out a date yourself and never re-read the ISO date: say date_spoken translated into the caller\\'s language, and say today or tomorrow only when relative_day says so.';\nconst r = $input.first().json;\nconst q = $('BOOK Validate Input').first().json;\nconst s = $('BOOK Find Slot').first().json;\nconst p = $('BOOK Match Patient').first().json;\nconst cal = { doctor: s.doctor, location: s.location };\nconst linked = p.patient_match === 'existing_patient';\n// ---- reschedule hand-off (2026-09-14) ----\n// If manage_appointment marked an appointment for reschedule earlier in THIS call, that record is\n// still holding its old slot (evolvo state 3 does not release it). Hand it to the cancel step that\n// follows, but only once the replacement booking has actually succeeded.\nconst digitsOnly = x => (x || '').toString().replace(/\\D/g, '').replace(/^0+/, '');\nconst samePhone = (a, b) => { const x = digitsOnly(a), y = digitsOnly(b); return !!(x && y && (x === y || x.endsWith(y) || y.endsWith(x))); };\nfunction takePendingResched() {\n  const sd = $getWorkflowStaticData('global'); sd.pendingResched = sd.pendingResched || {};\n  const now = Date.now();\n  for (const k of Object.keys(sd.pendingResched)) if (now - sd.pendingResched[k].ts > 6 * 3600 * 1000) delete sd.pendingResched[k];\n  const conv = q.conversation_id;\n  if (!conv || conv === 'anon') return null;\n  const e = sd.pendingResched[conv];\n  if (!e) return null;\n  // Consume it whatever happens next, so a second booking in the same call cannot re-fire the cancel.\n  delete sd.pendingResched[conv];\n  if (!e.scheduleid) return null;\n  // Same call, but a different number => a different person is being booked. Leave their record alone.\n  if (!samePhone(e.phone, q.phone)) return null;\n  return e;\n}\nif (r.statusCode === 200 && r.body && r.body.Result === 'OK') {\n  const old = takePendingResched();\n  return [{ json: { success: true,\n    booked: { doctor: cal.doctor, ...(s.service ? { service: s.service } : {}), location: cal.location,\n      date: s.date, ...dateLabels(s.date), time: s.time,\n      patient: q.full_name, phone: q.phone },\n    booking_mode: linked ? 'appointment_linked_to_existing_patient' : 'lead_pending_staff_confirmation',\n    patient_match: p.patient_match,\n    note: (linked\n      ? 'Appointment booked directly on the patient\\'s record. Tell the caller it is booked.'\n      : 'Booking registered as a request. Tell the caller the clinic will confirm it.') + ' ' + SPEAK_DATES,\n    _resched_scheduleid: old ? old.scheduleid : '',\n    _resched_old: old || null,\n    _resched_obs: old ? ('Reprogramat la ' + s.date + ' ' + s.time + ' (asistent virtual)') : '' } }];\n}\nif (r.statusCode === 401) {\n  return [{ json: { success: false, error: 'write_rejected_401', status: 401,\n    detail: 'post_schedule.php returned 401. Known cause: an empty required field (e.g. patientEmail). Check the request fields.',\n    hint: 'Tell the caller the booking system is temporarily unavailable and staff will call back.' } }];\n}\nreturn [{ json: { success: false, error: 'booking_failed', status: r.statusCode, api_response: r.body,\n  patient_match: p.patient_match,\n  hint: 'The slot may have just been taken. Re-check availability.' } }];"
  },
  {
   "name": "FIND Store Token",
   "jsCode": "const r = $input.first().json;\nif (r.statusCode !== 200 || !r.body || !r.body.token) {\n  return [{ json: { _error: true, success: false, error: 'auth_failed', status: r.statusCode,\n    hint: 'Could not authenticate to the clinic system. Tell the caller to try again shortly.' } }];\n}\nconst sd = $getWorkflowStaticData('global');\nsd.token = r.body.token;\nsd.tokenTs = Date.now();\nreturn [{ json: { token: sd.token } }];"
  },
  {
   "name": "MAN Store Token",
   "jsCode": "const r = $input.first().json;\nif (r.statusCode !== 200 || !r.body || !r.body.token) {\n  return [{ json: { _error: true, success: false, error: 'auth_failed', status: r.statusCode,\n    hint: 'Could not authenticate to the clinic system. Tell the caller to try again shortly.' } }];\n}\nconst sd = $getWorkflowStaticData('global');\nsd.token = r.body.token;\nsd.tokenTs = Date.now();\nreturn [{ json: { token: sd.token } }];"
  },
  {
   "name": "MAN Find Target",
   "jsCode": "// ---- calendar name vs service (2026-09-16) ----\n// One calendar of 30 carries its service in its NAME: \"Dr. Prof. Szekely Attila  - Consiliere /\n// Terapie psiho-ortoptica\" (note the double space). Returning that whole string as `doctor` made the\n// agent read the service suffix out loud, and pass it back into booking. Split it: `doctor` is the\n// speakable name, `service` is the suffix, and the name matcher resolves either form to the same\n// single calendar (verified against all 30 live names).\nconst splitName = raw => {\n  const s = (raw || '').toString().replace(/\\s+/g, ' ').trim();\n  const m = s.match(/^(.*?)\\s+[-\\u2013\\u2014]\\s+(.*)$/);\n  // No `service` key at all when there is no suffix, so 29 of 30 calendars are unchanged.\n  return m ? { doctor: m[1].trim(), service: m[2].trim() } : { doctor: s };\n};\n// ---- spoken date labels (2026-09-15) ----\n// A live call read \"2026-09-16 15:40\" out as \"astazi, marti 15 septembrie\" and the next slot\n// (2026-09-17 10:00) as \"maine, miercuri 16 septembrie\" - the LLM was converting ISO dates to\n// spoken dates itself and was a day out, twice in one call, weekday name included. It never has to\n// do that arithmetic: every date we emit now carries date_spoken (weekday + day + month in\n// English, for the agent to translate) and relative_day (today / tomorrow / in N days), so\n// speaking a date is a translation, not a calculation.\nconst _WD = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];\nconst _MO = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];\nconst _todayISO = new Date().toLocaleDateString('en-CA', { timeZone: 'Europe/Bucharest' });\nconst _dayNo = iso => Math.round(Date.parse(iso + 'T12:00:00Z') / 86400000);\nfunction dateLabels(iso) {\n  iso = (iso || '').toString().slice(0, 10);\n  if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(iso)) return {};\n  const d = new Date(iso + 'T12:00:00Z');\n  const diff = _dayNo(iso) - _dayNo(_todayISO);\n  return {\n    date_spoken: _WD[d.getUTCDay()] + ' ' + d.getUTCDate() + ' ' + _MO[d.getUTCMonth()],\n    relative_day: diff === 0 ? 'today' : diff === 1 ? 'tomorrow' : diff === 2 ? 'the day after tomorrow'\n      : diff < 0 ? 'IN THE PAST - do not offer this' : 'in ' + diff + ' days',\n  };\n}\nconst SPEAK_DATES = 'Never work out a date yourself and never re-read the ISO date: say date_spoken translated into the caller\\'s language, and say today or tomorrow only when relative_day says so.';\n// Merge get_schedule_patient.php (patient-linked appointments) with a get_schedule.php scan\n// (catches Agenda/lead records, which get_schedule_patient.php does not return by phone - verified 2026-09-07).\n// 2026-09-09: matching is BY PHONE ONLY - the caller's spoken name is no longer used to filter,\n// because ASR mangles names badly. Every appointment on the phone is returned with the patient\n// name evolvo has on the record, and the agent must get an explicit yes on ONE of them.\nconst norm = s => (s || '').toString().toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '');\nconst digits = p => { let d = (p || '').toString().replace(/\\D/g, ''); if (d.startsWith('0040')) d = d.slice(4); else if (d.startsWith('40') && d.length > 10) d = d.slice(2); return d.replace(/^0+/, ''); };\nconst samePhone = (a, b) => { const x = digits(a), y = digits(b); return x && y && (x === y || x.endsWith(y) || y.endsWith(x)); };\nconst nameOf = rec => rec.pacient || rec.patient || rec.name || '';\nconst STATE = { 'programat': 'scheduled', 'confirmat': 'confirmed', 'anulat': 'cancelled', 'incheiat': 'completed', 'nu s-a prezentat': 'no_show', 'trebuie reprogramat': 'needs_reschedule', 'reprogramat': 'rescheduled' };\nconst stateOf = rec => STATE[norm(rec.state)] || rec.state || null;\nconst isCancelled = rec => stateOf(rec) === 'cancelled';\n// Short, stable handle for one record: derived from the scheduleid, so it survives between the\n// find call and the manage call and does not depend on list order (an index would shift if a\n// record appeared or disappeared in between - and that would cancel the wrong appointment).\nconst hashRef = key => { let h = 5381; for (let i = 0; i < key.length; i++) h = ((h * 33) ^ key.charCodeAt(i)) >>> 0; return h.toString(36).toUpperCase().slice(-4).padStart(4, '0'); };\nfunction collect(q, patientResp, scanResps) {\n  let recs = [];\n  const pb = patientResp && patientResp.body;\n  if (patientResp && patientResp.statusCode === 200 && pb && pb.Result !== 'NOTFOUND') {\n    recs = recs.concat(Array.isArray(pb) ? pb : (pb.Records || pb.records || []));\n  }\n  let scanErrors = 0;\n  for (const r of scanResps) {\n    if (r.statusCode === 200 && Array.isArray(r.body)) recs = recs.concat(r.body.filter(x => samePhone(x.phone, q.phone)));\n    else scanErrors++;\n  }\n  const seen = new Set();\n  recs = recs.filter(x => { const k = x.scheduleid || (x.date + '|' + x.medic); if (seen.has(k)) return false; seen.add(k); return true; });\n  recs = recs.filter(r => !isCancelled(r));\n  recs.sort((a, b) => (a.date || '').localeCompare(b.date || ''));\n  const usedRefs = new Set();\n  for (const r of recs) {\n    let ref = hashRef(r.scheduleid || ((r.date || '') + '|' + (r.medic || '')));\n    let n = 1; while (usedRefs.has(ref)) ref = ref.slice(0, 3) + (n++).toString(36).toUpperCase();\n    usedRefs.add(ref); r._ref = ref;\n  }\n  return { recs, scanErrors };\n}\nconst publicView = rec => ({\n  ref: rec._ref,\n  person: nameOf(rec),\n  date: (rec.date || '').slice(0, 10),\n  ...dateLabels(rec.date),\n  time: (rec.date || '').slice(11, 16),\n  ...splitName(rec.medic || rec.doctor),\n  location: rec.workstation,\n  type: rec.type || null,\n  state: stateOf(rec),\n  kind: rec.form === 'Agenda' ? 'request_pending_staff_confirmation' : 'appointment',\n  manageable: Boolean(rec.scheduleid)\n});\n\nconst q = $('MAN Validate Input').first().json;\n// Either FIND's scan was reused (MAN Scan Cached? routed straight here and the evolvo nodes never\n// ran, so referencing them would throw) or we scanned ourselves.\nlet cached = null;\ntry { const cc = $('MAN Scan Cache').first().json; if (cc && cc._cache_hit && Array.isArray(cc._cached_recs)) cached = cc._cached_recs; } catch (e) {}\nlet recs;\nif (cached) {\n  recs = cached;  // already deduped, cancelled ones dropped, sorted, each carrying its _ref\n} else {\n  const pr = $('MAN get_schedule_patient.php').first().json;\n  const scans = $('MAN get_schedule.php').all().map(i => i.json);\n  if (pr.statusCode !== 200 && scans.every(r => r.statusCode !== 200)) {\n    return [{ json: { _error: true, success: false, error: 'evolvo_api_error', step: 'find', status: pr.statusCode } }];\n  }\n  recs = collect(q, pr, scans).recs;\n}\n// NOTE 2026-09-07: evolvo update_schedule.php rejects state 0 ('unknown status'), so a cancelled record cannot be set back to Programat via the API - no 'restore' action possible.\n\nif (!recs.length) {\n  return [{ json: { _error: true, success: false, error: 'not_found',\n    message: 'No appointments found for this phone number in the next 31 days.' } }];\n}\nlet target = null;\nif (q.ref) {\n  target = recs.find(r => r._ref === q.ref) || null;\n  if (!target) {\n    return [{ json: { _error: true, success: false, error: 'appointment_not_found',\n      message: 'That appointment reference is not among the appointments on this phone number any more.',\n      appointments: recs.map(publicView) } }];\n  }\n  // Cross-check: if the agent also sent the date it read out to the caller, it must be the date of\n  // the record the ref points at. Guards against a ref copied from the wrong line of the list.\n  if (q.date && !(target.date || '').startsWith(q.date)) {\n    return [{ json: { _error: true, success: false, error: 'ref_date_mismatch',\n      message: 'Nothing was changed: the appointment reference and the date you sent point at different appointments.',\n      hint: 'Read the list again, pick the ONE line the caller confirmed, and send that line\\'s ref together with that line\\'s date.',\n      appointments: recs.map(publicView) } }];\n  }\n} else {\n  const onDate = recs.filter(r => (r.date || '').startsWith(q.date));\n  if (!onDate.length) {\n    return [{ json: { _error: true, success: false, error: 'appointment_not_found',\n      message: 'No appointment on that date for this phone number.',\n      appointments: recs.map(publicView) } }];\n  }\n  if (onDate.length > 1) {\n    return [{ json: { _error: true, success: false, error: 'ambiguous_appointment',\n      message: 'Several appointments sit on that date for this phone number. Nothing was changed.',\n      hint: 'Read them back one at a time (name on the appointment, time, doctor) and ask which one the caller means, then call again with that appointment ref.',\n      appointments: onDate.map(publicView) } }];\n  }\n  target = onDate[0];\n}\n// ---- appointment read-back gate (server-side) ----\n// Identification is phone-only now, so the caller's explicit yes on THIS appointment is the whole\n// safeguard against touching a family member's booking. The agent reports that yes with\n// appointment_confirmed=true; a call without it is refused with the exact wording to read back,\n// and a call that flips the flag less than 5 s after the refusal is refused again (no real\n// read-back could fit in that time). Same pattern as the phone_confirmed gate. There is no undo:\n// evolvo cannot restore a cancelled record.\n{\n  const sd = $getWorkflowStaticData('global'); sd.apptGate = sd.apptGate || {};\n  const now = Date.now();\n  for (const k of Object.keys(sd.apptGate)) if (now - sd.apptGate[k] > 2 * 3600 * 1000) delete sd.apptGate[k];\n  const gateKey = (q.conversation_id || 'anon') + '|' + target._ref + '|' + q.action;\n  const firstRefusal = sd.apptGate[gateKey];\n  if (!q.appointment_confirmed || (firstRefusal && now - firstRefusal < 5000)) {\n    if (!firstRefusal) sd.apptGate[gateKey] = now;\n    const v = publicView(target);\n    return [{ json: { _error: true, success: false, error: 'confirm_appointment_first',\n      appointment: v,\n      read_back: v.person + ' — ' + (v.date_spoken || v.date) + ' ' + v.time + (v.doctor ? ' (' + v.doctor + ')' : ''),\n      message: 'NOTHING WAS CHANGED. ' + SPEAK_DATES + ' First name this one appointment to the caller in their own language - the name on the appointment, the weekday, the date and the time - ask whether that is the one they want changed, and END your turn. Only after the caller answers yes, call this tool again with exactly the same ref and action and appointment_confirmed set to true. If the caller says it is the wrong one, call evolvo_find_appointments again or use another ref from the list.' } }];\n  }\n}\nif (!target.scheduleid) {\n  return [{ json: { _error: true, success: false, error: 'not_supported_yet',\n    message: 'The clinic API does not expose the id needed to modify this appointment. Tell the caller the clinic staff will handle the change and their request has been noted.' } }];\n}\nreturn [{ json: { scheduleid: target.scheduleid, state: q.state, obs: q.obs, action: q.action,\n  summary: publicView(target) } }];\n"
  },
  {
   "name": "BOOK Match Patient",
   "jsCode": "const q = $('BOOK Validate Input').first().json;\nconst s = $('BOOK Find Slot').first().json;\nconst r = $input.first().json;\nconst base = { slotid: s.slotid, date: s.date, time: s.time };\n// Any lookup problem => fall back to the lead/agenda booking (never block the call on this step)\nif (r.statusCode !== 200 || !r.body) {\n  return [{ json: { ...base, patientid: '', patient_match: 'lookup_failed', lookup_status: r.statusCode } }];\n}\nconst b = r.body;\nlet recs = Array.isArray(b) ? b : (b.Records || b.records || b.Patients || b.patients || []);\nif (b && b.Result === 'NOTFOUND') recs = [];\nif (!Array.isArray(recs) || !recs.length) {\n  return [{ json: { ...base, patientid: '', patient_match: 'new_patient' } }];\n}\nconst norm = x => (x || '').toString().toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '');\nconst nameOf = rec => rec.pacient || rec.patient || rec.name || rec.patientName || rec.fullname ||\n  [rec.firstname, rec.lastname, rec.first_name, rec.last_name, rec.nume, rec.prenume].filter(Boolean).join(' ');\nconst qw = norm(q.full_name).split(/\\s+/).filter(Boolean);\nconst withId = recs.filter(x => x && x.patientid);\n// Never link a patient record on the phone alone: without a usable name, book as a lead for staff to reconcile.\nif (!qw.length) {\n  return [{ json: { ...base, patientid: '', patient_match: 'name_unverifiable', known_names: withId.map(nameOf).filter(Boolean) } }];\n}\nconst mine = withId.filter(rec => { const n = norm(nameOf(rec)); return n && qw.every(wd => n.includes(wd)); });\nif (mine.length === 1) {\n  return [{ json: { ...base, patientid: String(mine[0].patientid), patient_match: 'existing_patient',\n    matched_name: nameOf(mine[0]) } }];\n}\nif (mine.length > 1) {\n  return [{ json: { _error: true, success: false, error: 'ambiguous_patient',\n    message: 'Several patient records on this phone number match the given name.',\n    candidates: mine.map(nameOf),\n    hint: 'Ask the caller for their full name exactly as registered at the clinic, then call again.' } }];\n}\n// Phone is known but no record matches the name (family member / misspelling / API has no name field):\n// book as a lead so staff can reconcile; never attach to a guessed patientid.\nreturn [{ json: { ...base, patientid: '',\n  patient_match: withId.some(x => nameOf(x)) ? 'phone_known_name_mismatch' : 'name_unverifiable',\n  known_names: withId.map(nameOf).filter(Boolean),\n  lookup_fields: Object.keys(withId[0] || recs[0] || {}) } }];\n"
  },
  {
   "name": "LOG Format",
   "jsCode": "const q = $('LOG Validate Input').first().json;\nconst r = $input.first().json;\nif (r && r.error && !r.timestamp) {\n  return [{ json: { success: false, error: 'log_failed', detail: String(r.error).slice(0, 200),\n    hint: 'Tell the caller a colleague will call back, and give them the branch direct number from the knowledge base.' } }];\n}\nreturn [{ json: { success: true, logged: true, request_type: q.request_type, branch: q.branch || null,\n  routed_to: 'callback_sheet',\n  message: 'Request logged for the clinic team; a colleague will call the patient back. Do not promise a specific time or person, and do not give a reference number.' } }];\n"
  },
  {
   "name": "BOOK Resched Done",
   "jsCode": "// Final BOOK response. Strips the internal reschedule hand-off fields and, when an old appointment\n// had to be cancelled to free its slot, reports whether that cancellation actually went through.\nconst base = { ...$('BOOK Format Booking').first().json };\nconst sid = base._resched_scheduleid || '';\nconst old = base._resched_old || null;\ndelete base._resched_scheduleid; delete base._resched_old; delete base._resched_obs;\nif (!sid || !old) return [{ json: base }];\nconst r = $input.first().json || {};\nconst ok = r.statusCode === 200 && r.body && r.body.Result === 'OK';\nconst when = (old.date_spoken || old.date || '') + ' ' + (old.time || '');\nbase.replaced_appointment = { ref: old.ref, person: old.person, date: old.date,\n  date_spoken: old.date_spoken || '', time: old.time,\n  doctor: old.doctor, location: old.location, cancelled: !!ok };\nbase.note = ok\n  ? base.note + ' The earlier appointment (' + when + ') has been cancelled and its slot released.'\n  : base.note + ' IMPORTANT: the new time is booked, but the earlier appointment (' + when + ') could NOT be cancelled - tell the caller the clinic will remove the old one.';\nreturn [{ json: base }];"
  },
  {
   "name": "CA Display Names",
   "jsCode": "// Spoken/display provider names. Applied ONLY here, at the very last node before the\n// webhook responds, so nothing upstream ever sees a rewritten name.\n//\n// Why the last node and nowhere else: `MAN rel Find Calendar` matches a cancelled\n// appointment back to a calendar with `norm(c.name) === norm(a.doctor)` — an EXACT\n// equality after diacritic stripping. Diacritics survive it, but \"Anica\" vs \"Anca\"\n// does not. Rewriting the name in splitName (the obvious place) would silently turn\n// every slot_release_status into `unknown`. So: internal logic keeps the raw evolvo\n// name, the caller hears the corrected one.\n//\n// Matching is unaffected in the other direction too — `CA/BOOK Match Calendars`\n// NFD-normalise and strip combining marks on both sides, and the \"Anca\"->\"Anica\"\n// edit is within the Levenshtein<=2 budget, so the agent may echo a corrected name\n// straight back into a tool and it still matches. Verified against all 16 names.\nconst norm = s => (s || '').toString().toLowerCase().normalize('NFD')\n  .replace(/[̀-ͯ]/g, '').replace(/\\s+/g, ' ').trim();\n\n// Keyed on the normalised RAW evolvo name. Only names that actually change are listed;\n// the other ten come back untouched. Romanian names are deliberately left stripped\n// (clinic's call, 2026-09-21) — only Hungarian names get their diacritics back.\nconst DISPLAY = {\n  'dr. ilovan anca': 'Dr. Ilovan Anica',                                   // wrong given name, not a diacritic\n  'optometrist bodi ildiko': 'Optometrist Bódi Ildikó',\n  'optometrist ifj jeremias laszlo': 'Optometrist Ifj. Jeremiás László',   // note the restored period\n  'optometrist jeremias zoltan': 'Optometrist Jeremiás Zoltán',\n  // Both the split form (what the tools return) and the full raw form (what evolvo stores).\n  'dr. prof. szekely attila': 'Dr. Prof. Székely Attila',\n  'dr. prof. szekely attila - consiliere / terapie psiho-ortoptica':\n    'Dr. Prof. Székely Attila - Consiliere / Terapie psiho-ortoptica',\n};\n\n// Branch addresses. DIACRITICS ONLY — evolvo strips them from its own language, so the\n// Romanian voice was saying \"Mures\" on nearly every call. Abbreviations are deliberately\n// NOT expanded here: `Tg.` -> `Târgu` is three edits, over the matcher's Levenshtein\n// budget of 2, so an agent echoing the corrected string back into a tool would stop\n// matching (measured: all six Târgu branches went to NO MATCH). Expansion belongs in the\n// pronunciation dictionary, which is output-only and cannot affect matching.\n// Verified: all 8 corrected strings still round-trip through placeMatch.\nconst LOCATIONS = {\n  'tg. mures, str. postei nr. 3':       'Tg. Mureș, Str. Poștei Nr. 3',\n  'tg. mures, piata trandafirilor 53':  'Tg. Mureș, Piața Trandafirilor 53',\n  'tg. mures, piata republicii 5':      'Tg. Mureș, Piața Republicii 5',\n  'tg. mures, str. gheorghe doja 64-68':'Tg. Mureș, Str. Gheorghe Doja 64-68',\n  'tg. mures, bld. 1 dec 1918 nr 49':   'Tg. Mureș, Bld. 1 Dec 1918 Nr 49',\n  'tg. mures, fortuna':                 'Tg. Mureș, Fortuna',\n  'reghin, str. scolii 11':             'Reghin, Str. Școlii 11',\n  'sovata, str principala 196':         'Sovata, Str Principală 196',\n};\n\n// Every provider name evolvo held on 2026-09-21. A name arriving that is not in here\n// means the clinic renamed a calendar and DISPLAY needs revisiting — warn, never fail.\nconst KNOWN = new Set(['dr. ardelean adina','dr. baricz anna','dr. elekes ella','dr. ilovan anca',\n  'dr. istratuc dorina','dr. ormenisan delia maria','dr. petrea alexandra','dr. popa camelia',\n  'dr. prof. szekely attila','dr. prof. szekely attila - consiliere / terapie psiho-ortoptica',\n  'dr. rotar simona','dr. tripon robert','dr. zait natalia','optometrist bodi ildiko',\n  'optometrist dan laura','optometrist ifj jeremias laszlo','optometrist jeremias zoltan']);\n\n// Only keys that carry a provider name. Deliberately NOT a blind walk over every string:\n// patient names, observations and free text must never be touched.\nconst NAME_KEYS = new Set(['doctor', 'available_doctors', 'doctors_at_requested_location',\n  'matching_doctors', 'specialty_providers', 'calendar_name']);\nconst LOC_KEYS = new Set(['location', 'available_locations', 'doctor_locations',\n  'matching_locations', 'other_locations', 'requested_location']);\n\nconst unknown = new Set();\nconst one = v => {\n  if (typeof v !== 'string' || !v) return v;\n  const k = norm(v);\n  if (DISPLAY[k]) return DISPLAY[k];\n  if (!KNOWN.has(k)) unknown.add(v);\n  return v;\n};\nconst oneLoc = v => {\n  if (typeof v !== 'string' || !v) return v;\n  const k = norm(v);\n  if (LOCATIONS[k]) return LOCATIONS[k];\n  if (!Object.values(LOCATIONS).includes(v)) unknown.add(v);\n  return v;\n};\nconst walk = v => {\n  if (Array.isArray(v)) return v.map(walk);\n  if (v && typeof v === 'object') {\n    const o = {};\n    for (const [k, val] of Object.entries(v)) {\n      const f = NAME_KEYS.has(k) ? one : (LOC_KEYS.has(k) ? oneLoc : null);\n      o[k] = f ? (Array.isArray(val) ? val.map(f) : f(val)) : walk(val);\n    }\n    return o;\n  }\n  return v;\n};\n\n// Never break a live call over a cosmetic rewrite: on any failure, pass the input through.\nreturn $input.all().map(item => {\n  try { return { json: walk(item.json) }; }\n  catch (e) { console.log('display-names passthrough:', e.message); return item; }\n});\n"
  },
  {
   "name": "BOOK Display Names",
   "jsCode": "// Spoken/display provider names. Applied ONLY here, at the very last node before the\n// webhook responds, so nothing upstream ever sees a rewritten name.\n//\n// Why the last node and nowhere else: `MAN rel Find Calendar` matches a cancelled\n// appointment back to a calendar with `norm(c.name) === norm(a.doctor)` — an EXACT\n// equality after diacritic stripping. Diacritics survive it, but \"Anica\" vs \"Anca\"\n// does not. Rewriting the name in splitName (the obvious place) would silently turn\n// every slot_release_status into `unknown`. So: internal logic keeps the raw evolvo\n// name, the caller hears the corrected one.\n//\n// Matching is unaffected in the other direction too — `CA/BOOK Match Calendars`\n// NFD-normalise and strip combining marks on both sides, and the \"Anca\"->\"Anica\"\n// edit is within the Levenshtein<=2 budget, so the agent may echo a corrected name\n// straight back into a tool and it still matches. Verified against all 16 names.\nconst norm = s => (s || '').toString().toLowerCase().normalize('NFD')\n  .replace(/[̀-ͯ]/g, '').replace(/\\s+/g, ' ').trim();\n\n// Keyed on the normalised RAW evolvo name. Only names that actually change are listed;\n// the other ten come back untouched. Romanian names are deliberately left stripped\n// (clinic's call, 2026-09-21) — only Hungarian names get their diacritics back.\nconst DISPLAY = {\n  'dr. ilovan anca': 'Dr. Ilovan Anica',                                   // wrong given name, not a diacritic\n  'optometrist bodi ildiko': 'Optometrist Bódi Ildikó',\n  'optometrist ifj jeremias laszlo': 'Optometrist Ifj. Jeremiás László',   // note the restored period\n  'optometrist jeremias zoltan': 'Optometrist Jeremiás Zoltán',\n  // Both the split form (what the tools return) and the full raw form (what evolvo stores).\n  'dr. prof. szekely attila': 'Dr. Prof. Székely Attila',\n  'dr. prof. szekely attila - consiliere / terapie psiho-ortoptica':\n    'Dr. Prof. Székely Attila - Consiliere / Terapie psiho-ortoptica',\n};\n\n// Branch addresses. DIACRITICS ONLY — evolvo strips them from its own language, so the\n// Romanian voice was saying \"Mures\" on nearly every call. Abbreviations are deliberately\n// NOT expanded here: `Tg.` -> `Târgu` is three edits, over the matcher's Levenshtein\n// budget of 2, so an agent echoing the corrected string back into a tool would stop\n// matching (measured: all six Târgu branches went to NO MATCH). Expansion belongs in the\n// pronunciation dictionary, which is output-only and cannot affect matching.\n// Verified: all 8 corrected strings still round-trip through placeMatch.\nconst LOCATIONS = {\n  'tg. mures, str. postei nr. 3':       'Tg. Mureș, Str. Poștei Nr. 3',\n  'tg. mures, piata trandafirilor 53':  'Tg. Mureș, Piața Trandafirilor 53',\n  'tg. mures, piata republicii 5':      'Tg. Mureș, Piața Republicii 5',\n  'tg. mures, str. gheorghe doja 64-68':'Tg. Mureș, Str. Gheorghe Doja 64-68',\n  'tg. mures, bld. 1 dec 1918 nr 49':   'Tg. Mureș, Bld. 1 Dec 1918 Nr 49',\n  'tg. mures, fortuna':                 'Tg. Mureș, Fortuna',\n  'reghin, str. scolii 11':             'Reghin, Str. Școlii 11',\n  'sovata, str principala 196':         'Sovata, Str Principală 196',\n};\n\n// Every provider name evolvo held on 2026-09-21. A name arriving that is not in here\n// means the clinic renamed a calendar and DISPLAY needs revisiting — warn, never fail.\nconst KNOWN = new Set(['dr. ardelean adina','dr. baricz anna','dr. elekes ella','dr. ilovan anca',\n  'dr. istratuc dorina','dr. ormenisan delia maria','dr. petrea alexandra','dr. popa camelia',\n  'dr. prof. szekely attila','dr. prof. szekely attila - consiliere / terapie psiho-ortoptica',\n  'dr. rotar simona','dr. tripon robert','dr. zait natalia','optometrist bodi ildiko',\n  'optometrist dan laura','optometrist ifj jeremias laszlo','optometrist jeremias zoltan']);\n\n// Only keys that carry a provider name. Deliberately NOT a blind walk over every string:\n// patient names, observations and free text must never be touched.\nconst NAME_KEYS = new Set(['doctor', 'available_doctors', 'doctors_at_requested_location',\n  'matching_doctors', 'specialty_providers', 'calendar_name']);\nconst LOC_KEYS = new Set(['location', 'available_locations', 'doctor_locations',\n  'matching_locations', 'other_locations', 'requested_location']);\n\nconst unknown = new Set();\nconst one = v => {\n  if (typeof v !== 'string' || !v) return v;\n  const k = norm(v);\n  if (DISPLAY[k]) return DISPLAY[k];\n  if (!KNOWN.has(k)) unknown.add(v);\n  return v;\n};\nconst oneLoc = v => {\n  if (typeof v !== 'string' || !v) return v;\n  const k = norm(v);\n  if (LOCATIONS[k]) return LOCATIONS[k];\n  if (!Object.values(LOCATIONS).includes(v)) unknown.add(v);\n  return v;\n};\nconst walk = v => {\n  if (Array.isArray(v)) return v.map(walk);\n  if (v && typeof v === 'object') {\n    const o = {};\n    for (const [k, val] of Object.entries(v)) {\n      const f = NAME_KEYS.has(k) ? one : (LOC_KEYS.has(k) ? oneLoc : null);\n      o[k] = f ? (Array.isArray(val) ? val.map(f) : f(val)) : walk(val);\n    }\n    return o;\n  }\n  return v;\n};\n\n// Never break a live call over a cosmetic rewrite: on any failure, pass the input through.\nreturn $input.all().map(item => {\n  try { return { json: walk(item.json) }; }\n  catch (e) { console.log('display-names passthrough:', e.message); return item; }\n});\n"
  },
  {
   "name": "FIND Display Names",
   "jsCode": "// Spoken/display provider names. Applied ONLY here, at the very last node before the\n// webhook responds, so nothing upstream ever sees a rewritten name.\n//\n// Why the last node and nowhere else: `MAN rel Find Calendar` matches a cancelled\n// appointment back to a calendar with `norm(c.name) === norm(a.doctor)` — an EXACT\n// equality after diacritic stripping. Diacritics survive it, but \"Anica\" vs \"Anca\"\n// does not. Rewriting the name in splitName (the obvious place) would silently turn\n// every slot_release_status into `unknown`. So: internal logic keeps the raw evolvo\n// name, the caller hears the corrected one.\n//\n// Matching is unaffected in the other direction too — `CA/BOOK Match Calendars`\n// NFD-normalise and strip combining marks on both sides, and the \"Anca\"->\"Anica\"\n// edit is within the Levenshtein<=2 budget, so the agent may echo a corrected name\n// straight back into a tool and it still matches. Verified against all 16 names.\nconst norm = s => (s || '').toString().toLowerCase().normalize('NFD')\n  .replace(/[̀-ͯ]/g, '').replace(/\\s+/g, ' ').trim();\n\n// Keyed on the normalised RAW evolvo name. Only names that actually change are listed;\n// the other ten come back untouched. Romanian names are deliberately left stripped\n// (clinic's call, 2026-09-21) — only Hungarian names get their diacritics back.\nconst DISPLAY = {\n  'dr. ilovan anca': 'Dr. Ilovan Anica',                                   // wrong given name, not a diacritic\n  'optometrist bodi ildiko': 'Optometrist Bódi Ildikó',\n  'optometrist ifj jeremias laszlo': 'Optometrist Ifj. Jeremiás László',   // note the restored period\n  'optometrist jeremias zoltan': 'Optometrist Jeremiás Zoltán',\n  // Both the split form (what the tools return) and the full raw form (what evolvo stores).\n  'dr. prof. szekely attila': 'Dr. Prof. Székely Attila',\n  'dr. prof. szekely attila - consiliere / terapie psiho-ortoptica':\n    'Dr. Prof. Székely Attila - Consiliere / Terapie psiho-ortoptica',\n};\n\n// Branch addresses. DIACRITICS ONLY — evolvo strips them from its own language, so the\n// Romanian voice was saying \"Mures\" on nearly every call. Abbreviations are deliberately\n// NOT expanded here: `Tg.` -> `Târgu` is three edits, over the matcher's Levenshtein\n// budget of 2, so an agent echoing the corrected string back into a tool would stop\n// matching (measured: all six Târgu branches went to NO MATCH). Expansion belongs in the\n// pronunciation dictionary, which is output-only and cannot affect matching.\n// Verified: all 8 corrected strings still round-trip through placeMatch.\nconst LOCATIONS = {\n  'tg. mures, str. postei nr. 3':       'Tg. Mureș, Str. Poștei Nr. 3',\n  'tg. mures, piata trandafirilor 53':  'Tg. Mureș, Piața Trandafirilor 53',\n  'tg. mures, piata republicii 5':      'Tg. Mureș, Piața Republicii 5',\n  'tg. mures, str. gheorghe doja 64-68':'Tg. Mureș, Str. Gheorghe Doja 64-68',\n  'tg. mures, bld. 1 dec 1918 nr 49':   'Tg. Mureș, Bld. 1 Dec 1918 Nr 49',\n  'tg. mures, fortuna':                 'Tg. Mureș, Fortuna',\n  'reghin, str. scolii 11':             'Reghin, Str. Școlii 11',\n  'sovata, str principala 196':         'Sovata, Str Principală 196',\n};\n\n// Every provider name evolvo held on 2026-09-21. A name arriving that is not in here\n// means the clinic renamed a calendar and DISPLAY needs revisiting — warn, never fail.\nconst KNOWN = new Set(['dr. ardelean adina','dr. baricz anna','dr. elekes ella','dr. ilovan anca',\n  'dr. istratuc dorina','dr. ormenisan delia maria','dr. petrea alexandra','dr. popa camelia',\n  'dr. prof. szekely attila','dr. prof. szekely attila - consiliere / terapie psiho-ortoptica',\n  'dr. rotar simona','dr. tripon robert','dr. zait natalia','optometrist bodi ildiko',\n  'optometrist dan laura','optometrist ifj jeremias laszlo','optometrist jeremias zoltan']);\n\n// Only keys that carry a provider name. Deliberately NOT a blind walk over every string:\n// patient names, observations and free text must never be touched.\nconst NAME_KEYS = new Set(['doctor', 'available_doctors', 'doctors_at_requested_location',\n  'matching_doctors', 'specialty_providers', 'calendar_name']);\nconst LOC_KEYS = new Set(['location', 'available_locations', 'doctor_locations',\n  'matching_locations', 'other_locations', 'requested_location']);\n\nconst unknown = new Set();\nconst one = v => {\n  if (typeof v !== 'string' || !v) return v;\n  const k = norm(v);\n  if (DISPLAY[k]) return DISPLAY[k];\n  if (!KNOWN.has(k)) unknown.add(v);\n  return v;\n};\nconst oneLoc = v => {\n  if (typeof v !== 'string' || !v) return v;\n  const k = norm(v);\n  if (LOCATIONS[k]) return LOCATIONS[k];\n  if (!Object.values(LOCATIONS).includes(v)) unknown.add(v);\n  return v;\n};\nconst walk = v => {\n  if (Array.isArray(v)) return v.map(walk);\n  if (v && typeof v === 'object') {\n    const o = {};\n    for (const [k, val] of Object.entries(v)) {\n      const f = NAME_KEYS.has(k) ? one : (LOC_KEYS.has(k) ? oneLoc : null);\n      o[k] = f ? (Array.isArray(val) ? val.map(f) : f(val)) : walk(val);\n    }\n    return o;\n  }\n  return v;\n};\n\n// Never break a live call over a cosmetic rewrite: on any failure, pass the input through.\nreturn $input.all().map(item => {\n  try { return { json: walk(item.json) }; }\n  catch (e) { console.log('display-names passthrough:', e.message); return item; }\n});\n"
  },
  {
   "name": "MAN Display Names",
   "jsCode": "// Spoken/display provider names. Applied ONLY here, at the very last node before the\n// webhook responds, so nothing upstream ever sees a rewritten name.\n//\n// Why the last node and nowhere else: `MAN rel Find Calendar` matches a cancelled\n// appointment back to a calendar with `norm(c.name) === norm(a.doctor)` — an EXACT\n// equality after diacritic stripping. Diacritics survive it, but \"Anica\" vs \"Anca\"\n// does not. Rewriting the name in splitName (the obvious place) would silently turn\n// every slot_release_status into `unknown`. So: internal logic keeps the raw evolvo\n// name, the caller hears the corrected one.\n//\n// Matching is unaffected in the other direction too — `CA/BOOK Match Calendars`\n// NFD-normalise and strip combining marks on both sides, and the \"Anca\"->\"Anica\"\n// edit is within the Levenshtein<=2 budget, so the agent may echo a corrected name\n// straight back into a tool and it still matches. Verified against all 16 names.\nconst norm = s => (s || '').toString().toLowerCase().normalize('NFD')\n  .replace(/[̀-ͯ]/g, '').replace(/\\s+/g, ' ').trim();\n\n// Keyed on the normalised RAW evolvo name. Only names that actually change are listed;\n// the other ten come back untouched. Romanian names are deliberately left stripped\n// (clinic's call, 2026-09-21) — only Hungarian names get their diacritics back.\nconst DISPLAY = {\n  'dr. ilovan anca': 'Dr. Ilovan Anica',                                   // wrong given name, not a diacritic\n  'optometrist bodi ildiko': 'Optometrist Bódi Ildikó',\n  'optometrist ifj jeremias laszlo': 'Optometrist Ifj. Jeremiás László',   // note the restored period\n  'optometrist jeremias zoltan': 'Optometrist Jeremiás Zoltán',\n  // Both the split form (what the tools return) and the full raw form (what evolvo stores).\n  'dr. prof. szekely attila': 'Dr. Prof. Székely Attila',\n  'dr. prof. szekely attila - consiliere / terapie psiho-ortoptica':\n    'Dr. Prof. Székely Attila - Consiliere / Terapie psiho-ortoptica',\n};\n\n// Branch addresses. DIACRITICS ONLY — evolvo strips them from its own language, so the\n// Romanian voice was saying \"Mures\" on nearly every call. Abbreviations are deliberately\n// NOT expanded here: `Tg.` -> `Târgu` is three edits, over the matcher's Levenshtein\n// budget of 2, so an agent echoing the corrected string back into a tool would stop\n// matching (measured: all six Târgu branches went to NO MATCH). Expansion belongs in the\n// pronunciation dictionary, which is output-only and cannot affect matching.\n// Verified: all 8 corrected strings still round-trip through placeMatch.\nconst LOCATIONS = {\n  'tg. mures, str. postei nr. 3':       'Tg. Mureș, Str. Poștei Nr. 3',\n  'tg. mures, piata trandafirilor 53':  'Tg. Mureș, Piața Trandafirilor 53',\n  'tg. mures, piata republicii 5':      'Tg. Mureș, Piața Republicii 5',\n  'tg. mures, str. gheorghe doja 64-68':'Tg. Mureș, Str. Gheorghe Doja 64-68',\n  'tg. mures, bld. 1 dec 1918 nr 49':   'Tg. Mureș, Bld. 1 Dec 1918 Nr 49',\n  'tg. mures, fortuna':                 'Tg. Mureș, Fortuna',\n  'reghin, str. scolii 11':             'Reghin, Str. Școlii 11',\n  'sovata, str principala 196':         'Sovata, Str Principală 196',\n};\n\n// Every provider name evolvo held on 2026-09-21. A name arriving that is not in here\n// means the clinic renamed a calendar and DISPLAY needs revisiting — warn, never fail.\nconst KNOWN = new Set(['dr. ardelean adina','dr. baricz anna','dr. elekes ella','dr. ilovan anca',\n  'dr. istratuc dorina','dr. ormenisan delia maria','dr. petrea alexandra','dr. popa camelia',\n  'dr. prof. szekely attila','dr. prof. szekely attila - consiliere / terapie psiho-ortoptica',\n  'dr. rotar simona','dr. tripon robert','dr. zait natalia','optometrist bodi ildiko',\n  'optometrist dan laura','optometrist ifj jeremias laszlo','optometrist jeremias zoltan']);\n\n// Only keys that carry a provider name. Deliberately NOT a blind walk over every string:\n// patient names, observations and free text must never be touched.\nconst NAME_KEYS = new Set(['doctor', 'available_doctors', 'doctors_at_requested_location',\n  'matching_doctors', 'specialty_providers', 'calendar_name']);\nconst LOC_KEYS = new Set(['location', 'available_locations', 'doctor_locations',\n  'matching_locations', 'other_locations', 'requested_location']);\n\nconst unknown = new Set();\nconst one = v => {\n  if (typeof v !== 'string' || !v) return v;\n  const k = norm(v);\n  if (DISPLAY[k]) return DISPLAY[k];\n  if (!KNOWN.has(k)) unknown.add(v);\n  return v;\n};\nconst oneLoc = v => {\n  if (typeof v !== 'string' || !v) return v;\n  const k = norm(v);\n  if (LOCATIONS[k]) return LOCATIONS[k];\n  if (!Object.values(LOCATIONS).includes(v)) unknown.add(v);\n  return v;\n};\nconst walk = v => {\n  if (Array.isArray(v)) return v.map(walk);\n  if (v && typeof v === 'object') {\n    const o = {};\n    for (const [k, val] of Object.entries(v)) {\n      const f = NAME_KEYS.has(k) ? one : (LOC_KEYS.has(k) ? oneLoc : null);\n      o[k] = f ? (Array.isArray(val) ? val.map(f) : f(val)) : walk(val);\n    }\n    return o;\n  }\n  return v;\n};\n\n// Never break a live call over a cosmetic rewrite: on any failure, pass the input through.\nreturn $input.all().map(item => {\n  try { return { json: walk(item.json) }; }\n  catch (e) { console.log('display-names passthrough:', e.message); return item; }\n});\n"
  }
 ]
}